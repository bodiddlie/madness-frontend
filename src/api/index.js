import { signup, login } from './open-endpoints';
import {
  setupInterceptors,
  searchByTitle,
  getList,
  addGameToList,
} from './private-endpoints';

export {
  signup,
  login,
  searchByTitle,
  getList,
  addGameToList,
  setupInterceptors,
};
